class Solution:
    def isPalindrome(self, s: str) -> bool:
        x=s.lower()
        res=''
        for ch in x:
            if ch.isalnum():
                res+=ch        
        y=res[::-1]
        if y==res:
            return True
        else: 
            return False    
