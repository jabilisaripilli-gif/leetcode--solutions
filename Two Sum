class Solution:
    def twoSum(self, nums, target):
        ht={}
        ans=[]
        for i in range(len(nums)):
            key=target-nums[i]
            if key in ht:
                ans=[ht[key],i]
                break
            ht[nums[i]]=i
        return ans



    
