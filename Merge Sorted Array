class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        x=nums1[:m]
        x.sort()
        y=nums2[:n]
        y.sort()
        x.extend(y)
        nums1[:]=x
        nums1.sort()
        return nums1



        
